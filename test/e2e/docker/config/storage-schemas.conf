# Carbon silently drops a point older than the longest archive of the schema it
# matches. The suite writes points stamped from a recording, so the retention
# is generous and the resolution is one second, which is what a sub-second
# sampler's data deserves.
[default]
pattern = .*
retentions = 1s:6h,10s:7d,1m:30d
