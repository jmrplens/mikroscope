# Documentation

Generated by `site/scripts/gen-docs.mjs` from the pages of the documentation site: do not edit this file. Change the page, and its Spanish twin beside it, then run `pnpm run docs` in `site/`.

| File | What it covers |
| --- | --- |
| [Five minutes with a router](walkthrough.md) | An agent inside the router reading the shared kernel, a CLI outside it, and the four limits that cannot be engineered away. |
| [Installing the agent](install.md) | What `mikroscope install` does to a RouterOS device and in which order, and how `upgrade` and `uninstall` change or remove it without touching anything they did not create. |
| [What it costs and what it cannot see](limits.md) | What the agent costs on an RB5009, measured from its own cgroup, and how to measure it on a device that is not this one. |
| [Recording and triggered capture](record.md) | How to pull a window of full-rate samples to your machine, mark the moments in it, add the router’s own log, and draw it as a deterministic SVG. |
| [The collector and its sinks](sinks.md) | What `mikroscope forward` does between the agent and your stores — pull, merge, derive, fan out — and what it promises when a store is slow. |
| [Dashboards and alerts](dashboards.md) | What the InfluxDB 3, Prometheus, PostgreSQL, Graphite and Elasticsearch dashboards contain, section by section, in the order they are read, and why the five stores do not carry the same panels. |
| [How to read what it shows](playbooks.md) | Seven readings of kernel data from one RB5009 — a production fault found by accident, three events provoked on purpose, two that needed no provoking, and the idle shape they are read against. |
| [Security](security.md) | Which piece of mikroscope runs on the router and which on your host, what each one reaches, and where every credential lives. |
| [Reference](reference.md) | Every verb of the mikroscope CLI, every flag with its default, its range and the MIKROSCOPE_* variable that sets it, read from cmd/mikroscope. |
| [About the project](about.md) | What works end to end, what it costs on the one device it has run on, what was found and not fixed, and what the release publishes. |

Beside them, in the repository rather than on the site:

- [../README.md](../README.md): What the project is, in one page, with the numbers it leads on
- [../dashboards/README.md](../dashboards/README.md): The two committed Grafana dashboards and their alert rules as files, with the datasource settings, including the InfluxDB datasource's two secure fields
- [../site/](../site/): The source these files are generated from, and the site that serves it
